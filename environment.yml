name: yt-scraper
channels:
  - defaults
dependencies:
  - python=3.7*
  - pip
  - pip:
    - dataclasses_json
    - git+https://github.com/sns-sdks/python-youtube.git@master
    - git+https://github.com/nimaid/youtube-transcript-api.git@add-caption-type-option
